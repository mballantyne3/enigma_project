class Enigma

  # def encrypt(message, key, date)
    # encrypted_code = Hash.new
    # encrypted_code[message] = the encrypted String
    # encrypted_code[key] = the key used for encryption as a String
    # encrypted_code[date] = the date used for encryption as a String DDMMYY
  # end

  # def decrypt(decryption, key, date)
  #   decrypted_code = Hash.new
  #   decrypted_code[decryption] = the decrypted String
  #   decrypted_code[] = the key used for decryption as a String
  #   decrypted_code[] = the date used for decryption as a String DDMMYY
  # end

  def keys

  end
end
